
===== ARCHITECTURE.md:434-438 =====
  434: | **R14 — RESOLVED AS A REAL DEFECT, NOT A FLAG ARTEFACT (v0.3.1).** The host-prompt-cache hypothesis is **falsified**: `--cache-ram 0 --no-cache-idle-slots` changes nothing at the levels with equal n (conc 2: 10/32 → 9/32; conc 4
  435: | ~~R14 (v0.3.0 original entry)~~ | **BLOCKING (v0.3.0, measured 2026-08-14): concurrent leaf dispatch destroys answer quality.** Serial dispatch scores **31/32 correct**; at 2 calls in flight **7/32**; at 4, **2/32**; at 8, **7/1
  436: | **R13** | **CONFIRMED, MECHANISM FALSIFIED, MITIGATED-NOT-SOLVED (v0.2.6).** Paired control, one process, byte-identical prompts, same moment: shared pinned slot **24/54 leaked**, virgin slot **0/54** (Fisher p = 4.4e-9). It **s
  437: | R12 | Injection-shaped text in the analyzed corpus steers leaf extractions (a benchmark-robustness risk under the local single-user threat model, not a security hole) | Leaf outputs treated as untrusted data by the root's cross-
  438: 

===== ARCHITECTURE.md:251-256 =====
  251:    *Relationship to the §8 chunk-size lock:* the lock forbids sweeping RLM's main lever on benchmark-adjacent signal. It is not violated here — the benchmark does not exist yet (authoring is an S2 deliverable), and this evidence c
  252:    *Superseded prior text:* wall-clock per task at fixed quality, swept over {16K, 24K, 32K, 48K}.
  253:    **SWEEP RESULT (v0.2.5, 2026-08-13 — the chunk-size question was the wrong question).** 78 fact-present trials across {1K…32K} on a real leaf, one fresh process per cell (see R13). **Retrieval does not degrade with chunk size; 
  254:    **GEOMETRY LANDED (v0.3.0, 2026-08-14): `size_tokens: 640`, `stride_tokens: 480`, `overhead_tokens: 1920`, `max_subcalls: 926`.** Both v0.2.9 blockers are discharged (below), and the decision is made on re-measured cost, not on
  255:    **The cost claim was half right, and here is the honest version.** Over a 200,000-token corpus, run through the real C2 chunker on fixture prose with the shipped snap:
  256: 

===== ARCHITECTURE.md:422-429 =====
  422: | R1 | Root degrades under the scaffold (precedent: Prime Intellect's RLMEnv evaluation, Jan 2026 — untrained models incl. INTELLECT-3 scored *lower* inside an RLM harness on several benchmarks, though supplying a strategy flipped
  423: | R2 | iGPU prefill insufficient for leaf economics | S0 is a kill gate, not a benchmark; threshold is exact (§9) |
  424: | R3 | Prefix cache silently broken by prefix drift | **Instrument changed v0.3.0.** `tokens_cached` cannot serve as the drift detector — the inequality it was to be compared against is unmeetable at every window and every `-ub` (
  425: | R4 | MTP path immature | MTP merged into llama.cpp 2026-05 (PR #22673) but beta: single-slot only, draft-n ≤ 2, spotty Vulkan history. Optional variant; adopt only if benchmark success unchanged AND decode ≥1.4×, measured on-box
  426: | R5 | Leaves confabulate on chunks (plausible-but-wrong extractions) | **The planned mitigation is MEASURED NEAR-INERT (v0.2.5) and must be replaced.** All 66 non-refusal wrong answers from the sweep were classified by whether th
  427: Original candidate list, retained for the record: require the leaf to quote the span AND the entity the span is about, then check scaffold-side that the quoted entity matches the one asked about; require a refusal token when the c
  428: | R6 | Sandbox escape / resource abuse | No-network default (defined in C1), rlimits/Job Objects, per-episode interpreter, unconditional kill (C1, C5). Local single-user threat model — documented, not overengineered |
  429: | R7 | Qwen3.8-27B lands with different architecture (Qwen already switched to hybrid attention once between 3 and 3.6) | S5 checklist (now incl. attention layout); the incumbent stays resident during A/B (headroom, §4) |

===== ARCHITECTURE.md:96-106 =====
  96: **Host config (Linux path — see A2):** BIOS UMA carve-out at 512 MB; GTT raised via `ttm.pages_limit` / `ttm.page_pool_size` (~100 GiB; `amdgpu.gttsize` is deprecated on kernels ≥ 6.18); `--no-mmap` on both servers so weights land
  97: 
  98: **GEOMETRY LANDED (v0.3.0, 2026-08-14): `scaffold.chunk` ships `size_tokens: 640`, `stride_tokens: 480`, `overhead_tokens: 1920`, and `max_subcalls` rises 522 → 926.** The two blockers §7 #2 raised are both discharged — the 4.4× o
  99: 
  100: **INSTRUCTION DECAY — CONFIRMED, AND IT UNIFIES TWO EARLIER FINDINGS (v0.2.9, 2026-08-14). The layout is NOT the fix; the window size is.**
  101: Replication was exact (30/30 false positives at 1,024 vs 0/21 at 640, p = 8.74e-15). The confound is dead: at **matched density** (3 entity bindings at every size, larger chunks padded with entity-free filler, asserted at build ti
  102: **The mechanism, with zero free parameters.** In the shipped layout the instruction-to-generation distance is (window + question) ≈ **685 / 1,069 / 2,093** tokens at 640 / 1,024 / 2,048. §7 #2's independently measured *fact*-cliff
  103: **One horizon, two distances: instruction distance sets the answer/refuse CRITERION; needle distance sets RETRIEVABILITY.** The paraphrase column separates them cleanly — moving instructions close (arms B/C) pushes the *chunk* fur
  104: **Layout is therefore NOT the lever, and no layout ships.** Arms B (prefix repeated after the chunk) and C (prefix only after the chunk) cut false positives to ~2% but trade them for misses roughly 1:1 (recall 83% → 57% / 65%; MIS
  105: *Superseded hypothesis text follows, retained because the reasoning that motivated the experiment is part of the record.*
  106: **(v0.2.8, superseded) The layout below may be optimizing the wrong thing.** The refusal A/B produced an unplanned result with a larger claim than the experiment it came from: the **same** `leaf-prefix.v1.md`, unchanged, produces 

===== ARCHITECTURE.md:265-269 =====
  265: 
  266:    The "identical 1.333 overlap factor ⇒ free" reasoning was true of **chunk** tokens only, where 640/480 is in fact 2.0% *cheaper*. It is false of what the server actually prefills, because two per-call costs are **window-indepen
  267:    **Landed regardless, and the reason is that the cheap geometry buys nothing.** At 1,024 the leaf's own prefix sits past the horizon: 30/30 false positives, and `s2/OCCUPANCY.md` §8 reproduced the recall gap by accident on a cor
  268:    **Window count is NOT the formula the spec priced it with.** `ceil((T − size)/stride) + 1` assumes every window end lands exactly one stride after the last; C2's `_snap_back` moves ends **backward** to a boundary within the ±10
  269:    **BLOCKER 1 DISCHARGED — the 4.4× was a default flag, and it never discriminated between geometries (`s2/OCCUPANCY.md`, 1,416 calls, 24 conditions).** All of the growth is `latency_queue_ms`; prefill is flat (1,391 → 1,389 ms a

===== ARCHITECTURE.md:492-500 =====
  492: ## 14. Changelog
  493: 
  494: - **v0.3.3 — 2026-08-14.** The architecture ladder ran and is **INCONCLUSIVE — the control was confounded** (`s2/ARCH-LADDER.md`). Both the hybrid leaf and `gemma-4-12B-it` hold the refusal criterion at 640 and 1,024 chunk tokens 
  495: - **v0.3.2 — 2026-08-14.** Two tokenizer-level hypotheses tested directly against the live leaf (`s2/TOKENIZER.md`); both negative, and one **withdraws a recorded finding**. **(1) No double BOS.** The Qwen3.6 template emits no BOS
  496: - **v0.3.1 — 2026-08-14.** **R14 is a real defect, not a flag artefact, and it forces a rewrite of §2's performance corollary.** The host-prompt-cache hypothesis is falsified — `--cache-ram 0 --no-cache-idle-slots` changes nothing
  497: - **v0.3.0 — 2026-08-14.** Two blockers measured and cleared, the geometry landed on re-priced cost, and a third blocker found by the controls. **(1) The 4.4× per-call wall-clock effect is a default flag, not a mystery, and it nev
  498: - **v0.2.6 — 2026-08-13.** R13 reproduced under a paired control and its mechanism falsified. **Confirmed:** shared slot 24/54 vs virgin slot 0/54 (p=4.4e-9), surviving both a cold full re-prefill and `action=erase`; leak rate ris
  499: - **v0.2.5 — 2026-08-13.** The chunk-size sweep ran on real hardware and answered a different question than the one asked, while surfacing a blocking defect. **(1) New R13, BLOCKING: the leaf leaks content across requests on a sha
  500: - **v0.2.4 — 2026-08-13.** The leaf transport defect found by S1 was fixed (scaffold-side system prefix from the pinned registry + `/apply-template` before `/completion`, mirroring the root path), and measuring the now-working lea

===== s2/R14.md:1-10 =====
  1: # S2 — R14: concurrent leaf dispatch destroys the answer, and the host prompt cache is not why
  2: 
  3: **Harness:** `s2/run_occupancy.py` (the runner the occupancy controls found R14
  4: with — reused, not replaced, so the comparison is like-for-like) ·
  5: **drivers:** `s2/r14_ladder.ps1`, `s2/r14_hypotheses.ps1` ·
  6: **scorer:** `s2/analyse_r14.py` · **raw:** `s2/results/r14.jsonl` (1,152 calls,
  7: 15 conditions, 15 server launches, every one shut down by the runner).
  8: **Spec:** ARCHITECTURE.md v0.3.0 §2 corollary, §4 prefix & cache contract,
  9: §7 #1, §10 R13/R14. **Date:** 2026-08-14.
  10: 

===== s2/RESULTS.md:130-140 =====
  130: # Findings
  131: 
  132: Run on 2026-08-13, branch `capa1`, tree green at `79ae6a5` (263 tests) before the
  133: run. Leaf: `Qwen3.6-35B-A3B-UD-Q4_K_M` on ROCm, launched exactly as the S0
  134: launch contract specifies. No harness code was changed; the only additions were
  135: three throwaway PowerShell drivers (server start/stop, per-cell restart loop)
  136: that live in the session scratchpad and call `s2/*.py` unmodified. Every number
  137: below comes from a record in `s2/results/*.jsonl`.
  138: 
  139: Three things were found, in ascending order of how much they should worry us.
  140: The third one invalidates the way the sweep was originally scheduled, so it

===== s2/RESULTS.md:155-163 =====
  155: front of it with content from a chunk it had been shown minutes earlier.
  156: 
  157: Re-running the 4096 cell alone against a freshly launched server produced
  158: entirely different answers with no trace of the earlier cells
  159: (`s2/results/diag.jsonl`). The whole sweep was then re-run with **a freshly
  160: launched leaf process per cell**; that is what `s2/results/sweep.jsonl` and the
  161: table above are.
  162: 
  163: ### How far the leak goes

===== s2/RESULTS.md:166-178 =====
  166: cell:
  167: 
  168: | arm | launch flags added | reuse (`tokens_cached`) | leaked answers |
  169: |---|---|---|---|
  170: | ground truth | *fresh process per cell* | warm within a cell | **0 / 99** |
  171: | run 1 | none (S0 contract) | warm | 24 / 36 |
  172: | `--no-cache-idle-slots` | idle slots not cached | warm | 13 / 18 |
  173: | `--cache-ram 0` | host prompt cache off | warm | 9 / 9 |
  174: | `-ctxcp 0` | context checkpoints off | **none — `cache_n` 0 everywhere** | 4 / 9 |
  175: | `--cache-ram 0 -ctxcp 0` | both off | **none — `cache_n` 0 everywhere** | 5 / 9 |
  176: | virgin slot (`--slot 1`) | none | warm | **0 / 9** |
  177: | `POST /slots/0?action=erase` | — | — | **501 Not Implemented** |
  178: 

===== s2/R13-mitigations.md:38-48 =====
  38: ## 1. Option 1 — `cache_prompt: false`. Priced first and hardest. It is dead.
  39: 
  40: The instruction was: if the reproducer showed the bug does not occur under some
  41: ordinary condition, that is the cheap mitigation. So this was tested directly,
  42: on a virgin server, with the same corpus, probe and oracle that produce the
  43: positive control.
  44: 
  45: | arm | n | leaked | rate |
  46: |---|---|---|---|
  47: | shared slot, `cache_prompt: true` (control) | 18 | 15 | 83% |
  48: | **shared slot, `cache_prompt: false`** | 18 | **15** | **83%** |

===== s2/R13-mitigations.md:433-439 =====
  433: 
  434: ```
  435: llama-server --host 127.0.0.1 --port 8081 -m <leaf.gguf> \
  436:   -c 327680 -np 128 -ctk q8_0 -ctv q8_0 -fa on -ub 512 -b 2048 \
  437:   -lm none --no-kv-unified --cont-batching
  438: 
  439: # positive control: 15/18 on a shared slot

===== s2/ARCH-LADDER.md:1-14 =====
  1: # Is the horizon architectural? — INCONCLUSIVE, and the control was confounded
  2: 
  3: **Date:** 2026-08-14 · **Script:** `s2/arch_ladder.py` · **Raw:** `s2/results/arch_ladder_*.jsonl`
  4: 
  5: ## The question
  6: 
  7: The leaf (Qwen3.6-35B-A3B) is **hybrid**: ~25% of layers carry true KV attention,
  8: the rest compress history into a fixed-size, context-**independent** recurrent
  9: state (measured 62.8 MiB/slot regardless of context length). That is the shape
  10: that produces a hard horizon, and §4 measured facts *and* instructions failing at
  11: one shared threshold. If the horizon belongs to the architecture, then swapping
  12: to a full-attention `fallback_leaf` fixes it — and the spec reserves a config
  13: slot for exactly that. If it does not, that mitigation is dead and the horizon
  14: must be managed geometrically forever.

===== s2/cache_instrument.ps1:16-23 =====
  16: 
  17: # The shipped launch line: host prompt cache ON at its 8192 MiB default.
  18: Run-Cond -Name "default"
  19: # Two independent ways to switch the host cache off (s2/OCCUPANCY.md).
  20: Run-Cond -Name "cram0"       -Extra "--cache-ram 0"
  21: Run-Cond -Name "nocacheidle" -Extra "--no-cache-idle-slots"
  22: 
  23: Write-Host "==== all conditions complete ===="

===== s2/occupancy_conditions.ps1:24-34 =====
  24: 
  25: # HYPOTHESIS 4/5 -- the host prompt cache. Two independent ways to switch it off.
  26: Run-Cond -Name "cram0"       -Extra "--cache-ram 0"
  27: Run-Cond -Name "nocacheidle" -Extra "--no-cache-idle-slots"
  28: 
  29: # HYPOTHESIS 1 -- slot scanning by longest-common-prefix.
  30: Run-Cond -Name "sps0"        -Extra "-sps 0"
  31: 
  32: # SLOT INDEX vs CALL ORDINAL: shuffled slot order decorrelates the two.
  33: Run-Cond -Name "shuffle"     -Order shuffle
  34: 

===== s2/r14_ladder.ps1:18-36 =====
  18: Set-Location D:\PROJECTS\rlm-halo-framework
  19: 
  20: $Out = "s2/results/r14.jsonl"
  21: $CacheOff = "--cache-ram 0 --no-cache-idle-slots"
  22: 
  23: function Run-Arm {
  24:   param([string]$Name, [string]$Extra, [int]$Conc, [int]$Calls)
  25:   Write-Host "==== $Name (conc $Conc, n $Calls, extra='$Extra') ===="
  26:   uv run --python 3.12 s2/run_occupancy.py --condition $Name --extra="$Extra" `
  27:     --np 128 --ctx 327680 --calls $Calls --concurrency $Conc `
  28:     --out $Out --log "traces/logs/r14-$Name.log"
  29:   if ($LASTEXITCODE -ne 0) { throw "$Name failed with exit code $LASTEXITCODE" }
  30: }
  31: 
  32: foreach ($k in @(1, 2, 4)) { Run-Arm -Name "r14-A-conc$k" -Extra ""        -Conc $k -Calls 32 }
  33: Run-Arm -Name "r14-A-conc8" -Extra ""        -Conc 8 -Calls 128
  34: foreach ($k in @(1, 2, 4)) { Run-Arm -Name "r14-B-conc$k" -Extra $CacheOff -Conc $k -Calls 32 }
  35: Run-Arm -Name "r14-B-conc8" -Extra $CacheOff -Conc 8 -Calls 128
  36: 

===== s2/r14_hypotheses.ps1:18-22 =====
  18: 
  19: $Out = "s2/results/r14.jsonl"
  20: $CacheOff = "--cache-ram 0 --no-cache-idle-slots"
  21: 
  22: # NOT `-Args`: `$Args` is a PowerShell AUTOMATIC variable (the unbound-argument

===== s2/occupancy_conc.ps1:1-6 =====
  1: $ErrorActionPreference = 'Stop'
  2: Set-Location D:\PROJECTS\rlm-halo-framework
  3: foreach ($c in @(@{n='conc8-full'; e=''}, @{n='conc8-full-cram0'; e='--cache-ram 0'})) {
  4:   Write-Host ('==== ' + $c.n + ' ====')
  5:   uv run --python 3.12 s2/run_occupancy.py --condition $c.n --extra="$($c.e)" --np 128 --ctx 327680 --calls 128 --concurrency 8 --log ('traces/logs/occ-' + $c.n + '.log')
  6: }

===== s2/occupancy_ladder.ps1:1-7 =====
  1: $ErrorActionPreference = 'Stop'
  2: Set-Location D:\PROJECTS\rlm-halo-framework
  3: foreach ($k in @(1,2,3,4)) {
  4:   Write-Host ('==== conc' + $k + '-ladder ====')
  5:   uv run --python 3.12 s2/run_occupancy.py --condition ('conc' + $k + '-ladder') --extra='--cache-ram 0' --np 128 --ctx 327680 --calls 32 --concurrency $k --log ('traces/logs/occ-conc' + $k + '-ladder.log')
  6: }
  7: Write-Host '==== ladder complete ===='
