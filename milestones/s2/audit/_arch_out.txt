
---------- ARCHITECTURE.md:98 ----------
**GEOMETRY LANDED (v0.3.0, 2026-08-14): `scaffold.chunk` ships `size_tokens: 640`, `stride_tokens: 480`, `overhead_tokens: 1920`, and `max_subcalls` rises 522 → 926.** The two blockers §7 #2 raised are both discharged — the 4.4× occupancy effect is a default-flag pathology that does not discriminate between geometries (`milestones/s2/OCCUPANCY.md`), and `timings.cache_n` is sound (`milestones/s2/CACHE-INSTRUMENT.md`). The cost is re-priced honestly in §7 #2: free in *chunk* tokens, **+25% in served tokens and ~+33% in wall**, because the 311-token head and the 516-token re-query rollback are window-independent and 58% more windows pay 58% more of them. It is landed anyway, because at 1,024 the leaf cannot see the instructions it is being graded against and the cheaper geometry buys answers R5 says are no evidence of anything.

---------- ARCHITECTURE.md:100 ----------
**INSTRUCTION DECAY — CONFIRMED, AND IT UNIFIES TWO EARLIER FINDINGS (v0.2.9, 2026-08-14). The layout is NOT the fix; the window size is.**

---------- ARCHITECTURE.md:101 ----------
Replication was exact (30/30 false positives at 1,024 vs 0/21 at 640, p = 8.74e-15). The confound is dead: at **matched density** (3 entity bindings at every size, larger chunks padded with entity-free filler, asserted at build time) the false-positive rate goes **0/12 at 640 → 12/12 at 1,024** (p = 7.4e-07) → 12/12 at 2,048, while **density at fixed size does nothing — three separate contrasts at p = 1.0**.

---------- ARCHITECTURE.md:102 ----------
**The mechanism, with zero free parameters.** In the shipped layout the instruction-to-generation distance is (window + question) ≈ **685 / 1,069 / 2,093** tokens at 640 / 1,024 / 2,048. §7 #2's independently measured *fact*-cliff bracket is **[967 pass, 1022 fail]**. 685 sits inside the horizon → instructions obeyed → 0/24 false positives. **1,069 sits 47 tokens past the measured fail point → instructions ignored → 24/24.** So the shipped `window: 1024` failed at 100% rather than partially because it puts the system prefix a few dozen tokens on the wrong side of a cliff that was measured for facts, in a different experiment, before this one was designed. The same single horizon predicts all 8 admissible layout×size cells.

---------- ARCHITECTURE.md:103 ----------
**One horizon, two distances: instruction distance sets the answer/refuse CRITERION; needle distance sets RETRIEVABILITY.** The paraphrase column separates them cleanly — moving instructions close (arms B/C) pushes the *chunk* further away, and recall collapses to MISS (not confabulation) even though the needle is well inside the horizon.

---------- ARCHITECTURE.md:253 ----------
   **SWEEP RESULT (v0.2.5, 2026-08-13 — the chunk-size question was the wrong question).** 78 fact-present trials across {1K…32K} on a real leaf, one fresh process per cell (see R13). **Retrieval does not degrade with chunk size; it falls off a cliff at absolute DISTANCE from the needle to the question: 38/39 correct within ~1,000 tokens, 0/39 beyond it** (Fisher p ≈ 1e-21). Size is ruled out directly by the data's own counterexamples: a **32,768**-token chunk whose needle sits 967 tokens from the end scores **6/6**, while a **1,024**-token chunk whose needle sits 1,022 tokens from the end scores **0/3**. Relative depth is ruled out too (depth-50% works at 1K and 2K, fails at 4K+). The measured bracket is [967 pass, 1022 fail].

---------- ARCHITECTURE.md:267 ----------
   **Landed regardless, and the reason is that the cheap geometry buys nothing.** At 1,024 the leaf's own prefix sits past the horizon: 30/30 false positives, and `milestones/s2/OCCUPANCY.md` §8 reproduced the recall gap by accident on a corpus built for a different purpose (122/128 correct at 1,024 against 128/128 twice at 640, same prefix, same sampling, same documents). §10 R5 says a leaf answer is no evidence the fact was present; at 640 that stops being true (0/45 false positives, 45/45 literal recall). The +296 s is the price of the leaf answering the question it was asked.

---------- ARCHITECTURE.md:426 ----------
| R5 | Leaves confabulate on chunks (plausible-but-wrong extractions) | **The planned mitigation is MEASURED NEAR-INERT (v0.2.5) and must be replaced.** All 66 non-refusal wrong answers from the sweep were classified by whether the emitted identifier occurs verbatim in its own chunk: **59/66 (89%) DO.** The failure mode is not fabrication but *misattribution* — returning a DIFFERENT entity's real identifier from the same chunk (and at two cells, handing over the cell's own real key for an organisation not present). Every one of those has a genuine quotable sentence, so the spec's evidence-span check (`normalized span in normalized chunk`) passes: **catch rate 7/66 = 11%.** A span check verifies that text was copied, not that it answers the question. **Two of the three replacements are now MEASURED DEAD (v0.2.8), by a four-arm A/B over 387 calls:** (i) *a refusal-strengthened prefix* — `leaf-prefix.v2.md` scored 29/30 false positives against the control's 30/30, **p = 1.0**; and (ii) *an explicit `abstain` field* — the envelope produced **0 valid JSON in 180 replies** (plain prose, not malformed JSON), and in the 640-token cells where it did parse it made matters **worse**, 0/21 → 14/21, by emitting `abstain: false` alongside another entity's real key **with a verified evidence span on every one** — the span check and the abstain boolean failing on the same reply, which is the sharpest possible demonstration that neither observes whether the answer is right. The envelope also cost recall through format breakage (paraphrase 24/30 → 15/30, p = 0.029). **Ship `leaf_envelope.enabled: false`; keep `leaf-prefix.v1.md` pinned.** **(v0.2.9) k-way agreement is DOWNGRADED from "surviving candidate" to "measured weak"** — the instruction-decay result shows the leaf's answer/refuse behaviour is driven by a distance threshold shared across calls, so k independent calls on the same chunk sit on the same side of the same cliff and their errors are correlated rather than independent; k-way voting cannot average out a systematic criterion shift, and it costs k× a sub-call budget §8's corpus ceiling already constrains. **The primary R5 mitigation is now scaffold-side verification of leaf answers** — the root treating every leaf answer as a hypothesis to check in code (regex, counting, cross-referencing against the chunk it was drawn from), which is cheap, deterministic, and independent of the failure mode. **The cheapest mitigation of all is geometric: at a 640-token window the false-positive rate is 0/45 with 45/45 literal recall**, so keeping the window inside the horizon removes most of what R5 was invented to catch. Formerly: k-way agreement across independent leaf calls as an untested candidate — and note it costs k× the sub-call budget, which §8's per-category corpus ceiling already constrains. See also the instruction-decay finding in §4: if the false-positive rate is a distance artefact rather than a model property, the cheapest fix is geometric, not procedural, and would be measured by the follow-up pre-registered there.

---------- ARCHITECTURE.md:434 ----------
| **R14 — RESOLVED AS A REAL DEFECT, NOT A FLAG ARTEFACT (v0.3.1).** The host-prompt-cache hypothesis is **falsified**: `--cache-ram 0 --no-cache-idle-slots` changes nothing at the levels with equal n (conc 2: 10/32 → 9/32; conc 4: 5/32 → 6/32; both Fisher p = 1.0), and `cache_n` was 0 on every call in the cache-ON arm, so the cache was never mechanistically a candidate. Also **separated and rejected**: sampling (temp 0 → 6/128), streamed-abort handling (7/128 — the dispatcher needs no change), and batch geometry (24/128, 8/128). **Live lead:** `--no-cont-batching` at 2 in flight scores 27/32, statistical parity with serial (p = 0.196) against p = 1.08e-05 versus the batched arm — continuous batching is implicated — but it fails at 8. **Not separated:** concurrent-prefill slot bleed, which needs absolute timestamps the runner does not persist. **Mechanism evidence:** 17% of concurrent failures return their OWN document's true identifier with tokens dropped mid-string ("The record identifier stated in thecik", mid-phrase EOS at 6–9 predicted tokens with garbage subword tails) — token-level corruption of a correct decode, which is why R13's identifier detector is structurally blind to it (0 hits in 1,152 calls). **The audit added the finding that matters most: the corruption is BURSTY, not per-call** — correct answers arrive in contiguous runs of ~5–25 consecutive ordinals (permutation p < 1e-4 across four conditions), so the server enters and leaves a healthy regime. **Consequence for every statistic at concurrency 8: those cells are not 128 independent trials, effective n is closer to the burst count, and every Fisher p computed on them is anti-conservative** — a same-condition replicate pair at conc 8 differs at p = 0.013 on noise alone. The conc-2 and conc-4 results show no clustering and stand. Thermal and memory confounds are refuted by natural controls (a serial run started 13 s after a 724 s eviction storm scored 31/32; memory is identical at conc 1 and 2 while quality falls 31/32 → 9/32; quality *improves* across quartiles, the opposite sign from thermal drift). | **`scaffold.dispatch_concurrency: 1` — shipped.** Serial wins on *correct answers per second* by 2.7×–110× at every level; at 2 in flight fan-out buys 13% of wall-clock and costs 68% of the answers, so there is no operating point where parallelism pays. §2's corollary and §7 #1 are rewritten accordingly. **Do not treat this as closed:** the `--no-cont-batching` parity at conc 2 is a real lead worth a reproducer and an upstream issue (R13's treatment), and if batching is the cause then a fixed llama.cpp restores fan-out and with it the original performance thesis. Until then every wall-clock figure in this spec is a serial one, S2 gate (c) (fan-out speedup) may not be scored, and `max_wall_clock_s` may not be re-derived |

---------- ARCHITECTURE.md:436 ----------
| **R13** | **CONFIRMED, MECHANISM FALSIFIED, MITIGATED-NOT-SOLVED (v0.2.6).** Paired control, one process, byte-identical prompts, same moment: shared pinned slot **24/54 leaked**, virgin slot **0/54** (Fisher p = 4.4e-9). It **survives a cold full re-prefill** (`timings.cache_n == 0`) and **survives `action=erase`** returning 200 with a truthful `n_erased`, so the prompt cache is not the carrier. Leak rate rises monotonically with the number of documents a slot has previously held (0/9, 3/9, 6/9, 8/9, 8/9, 9/9). **The recurrent-state hypothesis is DEAD: a verified full-attention control (`gemma-4-12B-it`, arch `gemma4`, no `ssm.*` keys, loader allocates only non-SWA + SWA KV) leaked MORE than the hybrid — 39/54 vs 34/54.** This is not a Gated-DeltaNet problem and **R8 must not be recast as its cause**. Strongest single artifact is value-level and unguessable: a 1024-token window returned eight `ENT-#####:hex` pairs whose bindings are split verbatim between the current document and one previously held on that slot. Trigger is document **growth**, not size (uniform ~1K windows on a shared slot: 0/18; an ascending 256→2048 ladder: 4/18, first leak at 1,416 tokens) — so uniform chunking is NOT a safety property. Three corrections to the v0.2.5 record: `erase` is **not** 501 (501 only meant `--slot-save-path` was unset — and it does not fix the bug anyway); **`milestones/s2/results/sweep.jsonl` audits clean (117 calls, 0 foreign identifiers), so §7 #2's distance-cliff and false-positive findings STAND**; and the synthetic two-prompt design does **not** reproduce it (0/110) — reproduction needs the low-entropy fixture corpus plus an enumeration-style probe, stated as a limitation in the upstream draft. | **Prevention + detection, because prevention is bounded rather than proven.** (1) **Never reuse a slot**: `-np 128 -c 327680` (2,560 tok/slot) with one fresh slot per window and a process restart when the pool exhausts — a 200K corpus costs 3 restarts / ~20 s at the shipped 640/480 geometry (2 / 13.4 s at 1024/768), versus 4.9× for process-per-window. **`cache_prompt: false` (15/18 leaked) and `--parallel 1` (4/18) are NOT mitigations and are struck from the option list** — the first cannot work because the cache is not the carrier, the second makes reuse mandatory. (2) **Honest bound:** 138 virgin-slot calls with zero leaks gives a 95% upper bound of **2.2%**, not zero; a 200K episode is ~848 leaf calls at the shipped geometry, so the evidence permits ~19 contaminated answers per episode. Never write "leak-free" for this. (3) **Free deterministic detector, run on EVERY leaf call:** the scaffold holds every chunk, so any identifier-shaped token in an answer that is absent from the chunk sent and present in another chunk is a leak — zero model calls, one set-membership test. Strictly stronger than R5's evidence-span check (measured 11%) because it targets this failure exactly; its stated limits are paraphrased leakage and contaminated *refusals*. It doubles as S4's in-benchmark contamination monitor. (4) `-np 128` memory is UNMEASURED and must be measured before pinning (see §4). Upstream issue drafted: `milestones/s2/R13-upstream-draft.md` | In the chunk sweep, cells of ≥4096 tokens answered with keys and entity names belonging to the 1024- and 2048-token cells — strings absent from their own chunk. Not a scoring artifact: re-running with one FRESH leaf process per cell gave 0/99 leaks, against systematic leakage on the shared server. No configuration suppresses it — `--no-cache-idle-slots`, `--cache-ram 0`, `-ctxcp 0` (alone and together) all fail, and `/slots/{id}?action=erase` returns 501 on this build. Only a virgin slot or process is clean. **Production sends ~4 chunks per slot, so as built every fan-out episode past the first chunk may answer from a previous chunk's content.** Strongly suspected to be R8's root cause surfacing as a correctness bug rather than a performance one: both models are hybrid Gated-DeltaNet, whose per-slot RECURRENT state is not KV and may not be reset between requests. | **Nothing downstream of the leaf can be trusted until this is characterised.** Immediate: a minimal reproducer against llama.cpp b10375 (two prompts, one slot, second prompt answering from the first), then an upstream issue. Interim options, to be priced not assumed: one process per chunk (destroys the fan-out economics), `--parallel 1` with serial chunks, or a full-attention fallback leaf (already listed in config as the R8 hedge). The S2 gates and every leaf measurement recorded before this date must be re-run under whichever mitigation holds |

---------- ARCHITECTURE.md:494 ----------
- **v0.3.3 — 2026-08-14.** The architecture ladder ran and is **INCONCLUSIVE — the control was confounded** (`milestones/s2/ARCH-LADDER.md`). Both the hybrid leaf and `gemma-4-12B-it` hold the refusal criterion at 640 and 1,024 chunk tokens and lose it at 2,048 (ABSENT-refused 4/4, 4/4, 1/4 hybrid vs 4/4, 4/4, 2/4 control; LITERAL recall 4/4 everywhere, since the needle was planted at the end so needle-distance could not explain a failure). **But gemma is not a full-attention model:** the R13 verification that it has no recurrent state also recorded that its loader allocates "non-SWA + SWA KV caches", i.e. it interleaves SLIDING-WINDOW attention and therefore carries its own bounded-context mechanism. The two arms compare two different bounded-context mechanisms rather than bounded against unbounded, so the result is consistent with the horizon being mechanism-caused but **cannot show that a full-attention leaf would be free of it — the `fallback_leaf` mitigation is neither confirmed nor killed.** n=4 per cell, so 1/4 vs 2/4 is not a distinguishable difference, and this probe's threshold (2,048) is later than the validated harness's (1,024), making it a weaker instrument whose absolute numbers must not be quoted against the main results. **Methodological finding worth keeping:** the probe's first shape did not reproduce the phenomenon at all — one lone planted entity plus an obviously-foreign ABSENT question made refusal trivial (4/4 refusals at every size). The collapse appeared only with three planted bindings and an ABSENT question naming a plausible fourth entity from the same family. **The false-positive failure mode apparently requires plausible neighbours to hand over**, which is consistent with the R5 measurement that 59/66 wrong answers quote a real identifier from their own chunk — misattribution, not fabrication. A leaf with nothing to misattribute does not misattribute. Clean test still owed: a model with uniform global attention in every layer. None on disk qualifies today; **`Qwen3.8-27B` has appeared in the library and its attention layout is unverified — R7 already makes that day-one checklist item 2, and it is simultaneously the S5 swap target**, so verifying it settles two questions at once.

---------- ARCHITECTURE.md:497 ----------
- **v0.3.0 — 2026-08-14.** Two blockers measured and cleared, the geometry landed on re-priced cost, and a third blocker found by the controls. **(1) The 4.4× per-call wall-clock effect is a default flag, not a mystery, and it never discriminated between geometries** (`milestones/s2/OCCUPANCY.md`, 1,416 calls, 24 conditions, 0 leaks/mismatches/errors). All the growth is `latency_queue_ms`; prefill is flat across a 3.07× change in wall. `--cache-idle-slots` saves every idle slot to a host prompt cache on each new task while `--cache-ram`'s 8,192 MiB default holds 41 entries of this config's 202.80 MiB slot state against a 128-slot pool — past which evictions per request equal the occupancy **exactly**, at 37.5 ms each (7,268 evictions, 272 s of a 543 s run; the model predicts the run mean to 40 ms with no free parameters). `--cache-ram 0` removes it entirely and costs the warm re-query nothing; it is now pinned in `servers.leaf.extra_flags`. `-sps 0`, pool size, slot index and httpx limits are ruled out with controls. **(2) `timings.cache_n` is honest; §4's truth model was wrong** (`milestones/s2/CACHE-INSTRUMENT.md`, 373 calls). It never over-reported against what the process had seen (0/373); the 1,374-vs-3 anomaly is b10375's host cache restoring an idle slot's state onto a *different* slot, which also makes §4's "the prompt cache is per-slot — there is no cross-slot sharing" **a false sentence**, now corrected and made true by the same flag. With the flag on, reuse is an exact closed form — continuation reuses the full common prefix, a divergence reuses `N_resident − ub − 4` or nothing — **239/239 exact at two `-ub` values**, shipped as `rlm.dispatcher.predicted_reuse`. **§7 #3's three gates are re-derived** to forms that are measurable and *able to fail*: (a) becomes a sha256 + 311-token assertion on the rendered head plus an exact `cache_n == N_resident − ub − 4` on the re-query — the old inequality is unmeetable at every window and every `-ub` bar a one-token coincidence, and R3's detector moves with it; (b)'s >80% token-fraction target is retired (fixed by geometry, and buying it costs +50.2% of prefill per window at `-ub 128`) in favour of the same identity plus a priced ≤0.62× prefill ratio; (c) passes with equality on 14/14 root turns, which **partly discharges R8** — checkpoint invalidation does not occur for pure conversation growth. §7 #3 (d)'s 20–40× re-query lever is **re-priced to 1.72×** at the shipped window. **(3) GEOMETRY LANDED: window 640 / stride 480, overhead 1,920, `max_subcalls` 522 → 926** — and the "free in prefill" claim is corrected. Over a 200,000-token corpus it is free in *chunk* tokens (276,826 vs 282,475, −2.0%) and **not** free in served tokens (+25.2%) or wall (~1,180 s vs ~884 s serial, +33%), because the 311-token head and the 516-token re-query rollback are window-independent and 58% more windows pay 58% more of both. Landed regardless: at 1,024 the leaf's prefix sits past the horizon (30/30 false positives; the occupancy run reproduced the recall gap by accident, 122/128 vs 128/128 twice), so the cheaper geometry buys answers R5 says are no evidence of anything. **The window-count formula was measured wrong** — C2's backward snap shortens gaps, so the real chunker yields 424 windows against the formula's 417 (and 268 against 261 at the old geometry, which means the shipped `max_subcalls: 522` was already 14 calls short of its own geometry); `max_subcalls` is now derived from the bound no corpus can beat, `2 × ceil(200,000/432)`. **§8's corpus ceiling re-derived: the "full coverage of a 1M-char corpus is not affordable" verdict STANDS with a worse margin** — 552–614 windows, 1,104–1,228 sub-calls, `max_subcalls` still breaking first at ~75% coverage, the wall-clock breach growing ~955 s → ~1,560 s, and `max_total_tokens`'s margin halving; the aggregation cap drops from ~250K to ~200K tokens, with the wall clock binding earlier still. **(4) A THIRD BLOCKER, found by the occupancy controls: concurrent leaf dispatch destroys the answer.** 31/32 correct serial, 7/32 at 2 in flight, 2/32 at 4, 7/128 at 8 — degenerate text, not wrong answers, carrying no identifiers, so **R13's detector cannot see it**; reproduced with two independent OS processes on one server. `scaffold.dispatch_concurrency: 8` is shipped config and **was deliberately NOT changed here**: this is a defect of R13's class and gets R13's treatment (reproducer, mechanism, upstream issue) before a config edit picks a number. Consequences recorded rather than acted on: every wall-clock figure in this spec is now a serial one, S2 gate (c) may not be scored, and `max_wall_clock_s: 900` — breached at *both* geometries, so it does not discriminate between them either — may not be re-derived until the defect is characterised.
